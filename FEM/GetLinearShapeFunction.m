function N = GetLinearShapeFunction(xi, eta)
    %TODO: Implement the linear shape function vector (N). The vector has 
    %dimension [4,1] where N^k(xi, eta) are evaluated. You can construct this 
    %vector directly
end
