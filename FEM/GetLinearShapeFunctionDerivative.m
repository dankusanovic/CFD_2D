function N = GetLinearShapeFunctionDerivative(xi, eta)
    %TODO: Implement the linear shape function derivative matrix (N). The matrix 
    %has dimension [2,4] where the first row stores the derivatives with respect 
    %to xi and the second row with respect to eta.
end
